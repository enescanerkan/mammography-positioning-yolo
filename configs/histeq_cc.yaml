# =============================================================================
# Experiment Configuration: Histogram Equalization Strategy | CC View
# =============================================================================
strategy: "histeq"
view: "CC"

data_path: "data/raw"

model: "weights/yolov8l-pose.pt"

device: 0
workers: 8
batch: 16

epochs: 300
patience: 50
imgsz: 640
optimizer: "auto"

trained_model_path: ""
